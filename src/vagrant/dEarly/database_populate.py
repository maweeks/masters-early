# engine = create_engine('sqlite:///gamecatalog.db')
# Base.metadata.bind = engine
# DBSession = sessionmaker(bind=engine)
# session = DBSession()

# # Add DateTime
# user = User(timestamp=, count=)
# session.add(user)
# session.commit()

# print "Database populated."